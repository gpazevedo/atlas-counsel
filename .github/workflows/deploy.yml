name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write   # needed for OIDC → AWS credential exchange
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Preflight — require OIDC role ARN
        run: |
          if [ -z "${{ vars.AWS_ROLE_ARN }}" ]; then
            echo "::error::Repository variable AWS_ROLE_ARN is not set. Add it under" \
                 "Settings → Secrets and variables → Actions → Variables. Without it," \
                 "OIDC has no role to assume and credential loading fails."
            exit 1
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Verify AWS identity
        run: aws sts get-caller-identity

      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.ecr.outputs.registry }}
          ECR_REPOSITORY: atlas-counsel
        run: |
          docker build -t "$ECR_REGISTRY/$ECR_REPOSITORY:latest" \
                       -t "$ECR_REGISTRY/$ECR_REPOSITORY:${{ github.sha }}" .
          docker push "$ECR_REGISTRY/$ECR_REPOSITORY:latest"
          docker push "$ECR_REGISTRY/$ECR_REPOSITORY:${{ github.sha }}"

      - name: Deploy to ECS
        env:
          AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
        run: |
          aws ecs update-service \
            --cluster atlas-counsel \
            --service atlas-counsel \
            --region "$AWS_REGION" \
            --force-new-deployment
